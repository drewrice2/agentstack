Read this when adapting the minimal skill into a real AgentStack skill.

# Checklist

- Rename the directory and frontmatter `name` together.
- Start `description` with `Use when`.
- Rewrite the prompt into agent-facing instructions instead of pasting it
  verbatim.
- Keep only examples and references that add distinct context.
